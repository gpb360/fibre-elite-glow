[build]
  command = "rm -rf .next node_modules/.cache && pnpm install && pnpm build"

[build.environment]
  NEXT_TELEMETRY_DISABLED = "1"
  # Force cache busting
  BUILD_ID = "force-fresh-build-$(date +%s)"

[[plugins]]
  package = "@netlify/plugin-nextjs"